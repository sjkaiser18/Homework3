export class ProductReview{
    constructor(userName,ratingId,rating,comment,date){
        this.userName=userName;
         this.ratingId=ratingId;
         this.rating=rating;
         this.comment=comment;
         this.date=date;
    }
}
    export default ProductReview;